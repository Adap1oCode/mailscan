services:
  mailscan:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      MAILSCAN_API_KEY: ${MAILSCAN_API_KEY}
      TESSERACT_CMD: /usr/bin/tesseract
      PORT: ${PORT:-8000}
    restart: unless-stopped
